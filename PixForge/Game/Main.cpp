#include "Game.h"

int WinMain(){
  // //[FEATURE] Get data from file and set Game Window Title
  // PF::Core::Window window = PF::Core::Window("Game");
  // PF::Core::Renderer::Assets::init(&window);
  // PF::Core::Renderer::Objects::Manager::init("objects.bin", "textures.bin", &window);

  // PF::Game game = PF::Game(&window);
  // game.run();
  
  return 0;
}